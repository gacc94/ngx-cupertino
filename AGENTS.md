# AGENTS.md

## Context7 MCP
Always use the Context7 MCP (`context7` CLI or tools) to fetch current
documentation before writing any framework/library code.

## Package Manager
All commands MUST use `bun` — never `npm`, `yarn`, or `pnpm`.

## Monorepo
This is an Nx workspace. Use `bun nx` for all Nx commands.

## Figma MCP
Figma design specs are available via the Figma MCP.

## Notion MCP
Project documentation lives in Notion.
